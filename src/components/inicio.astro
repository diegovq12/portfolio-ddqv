---
import Button from "./button.astro";
---

<section id="inicio" class="inicio">
    <div class="content">
        <h1 class="title">
            Software Developer<br />
            <span class="name">Diego Quiros</span>
        </h1>
        <p class="description">
            Desarrollador web apasionado por crear soluciones digitales
            innovadoras, con enfoque en desarrollo móvil y web, y un interés
            creciente por la inteligencia artificial.
        </p>
        <div class="buttons">
            <Button
                text="LinkedIn"
                href="https://www.linkedin.com/in/diego-quiros-ba9a15294/"
                icon="https://cdn-icons-png.flaticon.com/512/174/174857.png"
                type="primary"
            />
            <Button
                text="GitHub"
                href="https://github.com/diegovq12"
                icon="https://cdn-icons-png.flaticon.com/512/25/25231.png"
                type="primary"
            />
        </div>
    </div>

    <div class="scroll-arrow">
        <a href="#proyectos">
            <img
                src="https://cdn-icons-png.flaticon.com/512/12581/12581924.png"
                alt="Scroll Down"
                style="transform: rotate(270deg); filter: invert(1); "
            />
        </a>
    </div>
</section>

<style>
    .inicio {
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 5rem 1rem;
        margin-bottom: 15.625rem; 
    }

    .content {
        max-width: 100%;
    }

    .title {
        font-size: 4rem; /* buena opcion 5rem*/
        font-weight: 700;
        margin-bottom: 0.5rem;
        line-height: 1.2;
        color: #9bb7d4;
        background: linear-gradient(-90deg, #9bb7d4, #9bb7d4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .name {
        color: #fff;
        background: linear-gradient(-90deg, #e6f1ff, #ffffff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        font-size: 6rem; /* buena opcion 7.5rem*/
        /* font-size: 96px; */
    }

    .description {
        font-size: 1.25rem;
        margin: 2rem 0;
        color: #e6f1ff;
        max-width: 600px; /* Set a max-width to center the text */
        margin-left: auto;
        margin-right: auto;
        text-wrap: pretty;
    }

    .buttons {
        display: flex;
        justify-content: center;
        gap: 1rem;
        margin-top: 2rem;
    }
    /* Estilo para la flecha */
    .scroll-arrow {
        position: absolute;
        bottom: 2rem;
        animation: bounce 1.5s infinite;
    }

    .scroll-arrow a img {
        width: 32px;
        height: 32px;
        cursor: pointer;
        transition: transform 0.3s ease;
    }

    .scroll-arrow a img:hover {
        transform: scale(1.2);
    }

    @keyframes bounce {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(8px);
        }
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .inicio .content > * {
        animation: fadeIn 1s ease-out forwards;
    }

    .inicio .title {
        animation-delay: 0.2s;
    }

    .inicio .description {
        animation-delay: 0.4s;
    }

    .inicio .buttons {
        animation-delay: 0.6s;
    }


    .scroll-arrow {
        animation-delay: 0.8s;
    }
</style>
